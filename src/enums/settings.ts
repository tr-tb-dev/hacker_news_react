export enum ThemeMode {
  Light = 'light',
  Dark = 'dark',
}

export enum Language {
  English = 'en',
  Hungarian = 'hu',
}

export enum ViewMode {
  List = 'list',
  Grid = 'grid',
}

export enum FontSize {
  Small = 14,
  Large = 16,
}
